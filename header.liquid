{%- liquid
  assign section_st = section.settings
  assign transparent_on_home = ''
  assign section_width = ''
  if section_st.section_width == 'fix_width'
    assign section_width = 'container'
  elsif section_st.section_width == 'expand_full_width'
    assign section_width = 'full-width px-20'
  else
    assign section_width = 'full-width-gap'
  endif
  assign transparent_mobile_class = ''
  if section_st.transparent_on_home and section_st.transparent_on_home
    assign transparent_mobile_class = ' transparent-mobile '
  endif
-%}
<script src="{{ 'localization-form.js' | asset_url }}" defer="defer"></script>
{%- if settings.cart_type == 'drawer' -%}
  <script src="{{ 'cart.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'cart-drawer.js' | asset_url }}" defer="defer"></script>
{%- endif -%}
{%- if settings.cart_type == 'added_message' -%}
  <script src="{{ 'cart-notification.js' | asset_url }}" defer="defer"></script>
{%- endif -%}
{%- if section_st.transparent_on_home -%}
  {%- assign transparent_on_home = ' transparent transition' -%}
{%- endif -%}
{%- capture style -%}
  {%- if section_st.uppercase_first_level -%}
    --menu-parent-text-transform: uppercase;
  {%- endif -%}
  {%- if section_st.header_text_icon_transparent != blank -%} 
    --header-transparent-color: {{ section_st.header_text_icon_transparent.red }}, {{ section_st.header_text_icon_transparent.green }}, {{ section_st.header_text_icon_transparent.blue }};
  {%- endif -%}
  --logo-width: {{ section_st.logo_width }}px; --logo-position: {{ section_st.logo_position }}; --logo-mobile-width: {{ section_st.logo_width_mobile }}px
{%- endcapture -%}
<script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
{% if section_st.sticky_header_type != 'none' -%}
  <sticky-header data-sticky-type="{{ section_st.sticky_header_type }}" class="header-wrapper">
{% else %}
  <div class="header-wrapper">
{% endif %}
<header
  class="color-{{ section_st.color_scheme }} gradient bls_header relative z-4{{ transparent_on_home }}{{ transparent_mobile_class }}{% if section_st.show_border_bottom and section_st.transparent_on_home %} {% if section_st.logo_position != 'menu-left' %}border-bottom{% endif %}{% endif %}"
  style="{{ style | strip | strip_newlines }}"
>
  <div class="{{ section_width }}">
    <header-inner class="d-grid align-center logo-{{ section_st.logo_position }}{% if section_st.logo_position == 'menu-left' %} logo-left border-bottom{% endif %}{% if section_st.show_border_bottom and section_st.transparent_on_home == blank %} border-bottom{% endif %}">
      <div class="header__logo text-center{% if section_st.logo_position != 'center' %} text-1025-unset{% endif %}">
        {% if request.page_type == 'index' %}
          <h1
            class="my-0{% if settings.heading_letter_spacing == '0.25em' %} fs-24{% else %} fs-40{% endif %} lh-normal heading-letter-spacing"
          >
        {% else %}
          <div
            class="h1 my-0{% if settings.heading_letter_spacing == '0.25em' %} fs-24{% else %} fs-40{% endif %} lh-normal heading-letter-spacing"
          >
        {%- endif -%}
        <a
          href="{{ routes.root_url }}"
          class="text-center header__heading-link link link--text remove-underline header-color heading-style"
          aria-label="{{ shop.name }}"
        >
          {%- liquid
            assign logo_img = ''
            assign logo_transparent_img = ''
            assign logo_img = section_st.logo
            assign logo_transparent_img = section_st.logo_transparent
          -%}
          {% if section_st.transparent_on_home and section_st.logo_transparent != blank %}
            {%- liquid
              assign img_transparent_alt = logo_transparent_img.alt | default: shop.name | escape
              assign logo_height = logo_transparent_img.width | divided_by: logo_transparent_img.aspect_ratio
            -%}
            {{
              logo_transparent_img
              | image_url: width: 600
              | image_tag:
                class: 'header__heading-logo transparent-logo',
                widths: '50, 100, 150, 200, 250, 300, 400, 500, 600',
                height: logo_height,
                width: logo_transparent_img.width,
                alt: img_transparent_alt,
                sizes: '100vw',
                fetchpriority: 'high'
            }}
          {% endif %}
          {%- if logo_img != blank -%}
            {%- assign img_alt = logo_img.alt | default: shop.name | escape -%}
            {%- assign logo_height = logo_img.width | divided_by: logo_img.aspect_ratio -%}
            {{
              logo_img
              | image_url: width: 600
              | image_tag:
                class: 'header__heading-logo',
                widths: '50, 100, 150, 200, 250, 300, 400, 500, 600',
                height: logo_height,
                width: logo_img.width,
                alt: img_alt,
                sizes: '100vw',
                fetchpriority: 'high'
            }}
          {%- else -%}
            <span class="site-logo">{{ shop.name }}</span>
          {%- endif -%}
        </a>
        {% if request.page_type == 'index' %}
          </h1>
        {% else %}
          </div>
        {%- endif -%}
      </div>
      {%- if section_st.menu != blank -%}
        <nav class="header__menu overlay-menu overlay-section">
          <toggle-menu data-item="overlay-menu" class="hidden-no-js mobile-menu-toggle">
            <button
              class="relative touch-target button btn-reset flex-column btn-close-menu"
              aria-haspopup="dialog"
              aria-label="{{ 'sections.header.expand_menu' | t }}"
              id="menu-bar-icon-bubble"
            >
              <span class="icon-menu first"></span>
              <span class="icon-menu second hidden"></span>
              <span class="icon-menu last"></span>
              <span class="absolute inset-0 opacity-0 icon-close z-1 d-inline-flex align-center justify-content-center">
                {% render 'icon-close', custom_width: 22 %}
              </span>
            </button>
          </toggle-menu>
          <input
            class="absolute inset-y-0 opacity-0 d-block d-1025-none side-menu hidden-js touch-target"
            type="checkbox"
            id="side-menu"
          >
          <label class="relative d-block d-1025-none hamb hidden-js btn-close-menu" for="side-menu">
            <span class="my-3 icon-menu first d-block"></span>
            <span class="my-3 icon-menu second d-block"></span>
            <span class="my-3 icon-menu last d-block"></span>
            <span class="absolute inset-0 opacity-0 icon-close z-1 d-inline-flex align-center justify-content-center">
              {% render 'icon-close', custom_width: 22 %}
            </span>
            <span class="visually-hidden">{{ 'sections.header.expand_menu' | t }}</span>
          </label>
          <main-menu class="mobile-menu-drawer d-flex{% if section_st.logo_position == 'left' %} justify-content-center{% endif %}">
            <div class="relative w-full! h-full mobile-menu-drawer--inner position-1025-unset d-flex flex-column">
              <div class="h-full overflow-x-hidden overflow-y-auto scroll-smooth">
                <ul
                  class="main-menu parent-menu d-1025-inline-flex flex-wrap list-unstyled m-0{% if section_st.logo_position == 'left' %} justify-content-center-1025{% endif %}"
                  role="list"
                  data-focus-item="menu-bar-icon-bubble"
                >
                  {% for link in section_st.menu.links %}
                    {%- if section.blocks.size > 0 -%}
                      {%- liquid
                        assign is_mega_menu = false
                        assign lastBlock = null
                        for block in section.blocks
                          if block.settings.text == link.title
                            assign lastBlock = block
                          endif
                        endfor
                      -%}
                      {% if lastBlock != null %}
                        {%- liquid
                          assign has_content = false
                        -%}
                        {%- case lastBlock.type -%}
                          {%- when 'menu_promotion' -%}
                            {%- for i in (1..2) -%}
                              {%- capture promotion -%}promotion_image_{{ i }}{%- endcapture -%}
                              {%- liquid
                                if lastBlock.settings[promotion] != blank
                                  assign has_content = true
                                endif
                              -%}
                            {%- endfor -%}
                          {%- when 'menu_collection' -%}
                            {%- for i in (1..6) -%}
                              {%- capture collection -%}collection_{{ i }}{%- endcapture -%}
                              {%- liquid
                                if lastBlock.settings[collection] != blank
                                  assign has_content = true
                                endif
                              -%}
                            {%- endfor -%}
                          {%- else -%}
                            {%- for i in (1..2) -%}
                              {%- capture product -%}product_{{ i }}{%- endcapture -%}
                              {%- liquid
                                if lastBlock.settings[product] != blank
                                  assign has_content = true
                                endif
                              -%}
                            {%- endfor -%}
                        {%- endcase -%}
                        <li
                          class="menu-link{% if link.links.size > 0 or has_content %} have-child mega-menu{%- endif -%}{% if link.links.size == 0 and has_content %} no-sub{%- endif -%}"
                          role="listitem"
                        >
                          <noscript>
                            <details>
                              <summary class="menu__item-link header-color-lg first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}">
                                <span class="link_text">{{ link.title }}</span>
                                {%- if link.links.size > 0 or has_content -%}
                                  <span class="absolute inset-0 right-0 w-full! open-children-toggle touch-target currentColor-svg">
                                    {% render 'icon-arrow-down', custom_width: 12 %}
                                  </span>
                                {%- endif -%}
                              </summary>
                              {%- if link.links.size > 0 or has_content -%}
                                <div class="inset-x-0 sub-menu absolute-1025 bg-default">
                                  <back-button
                                    tabindex="0"
                                    class="relative mb-20 d-block d-1025-none fs-small mt-15 hidden-no-js"
                                  >
                                    <div class="pt-5 pb-3 accent-color-second-bg rounded-corner-item px-15 d-inline-flex align-center heading-color pointer">
                                      <span class="absolute inset-y-0 icon-back left-10 d-inline-flex align-center lh-1 currentColor-svg">
                                        {%- render 'icon-prev', custom_width: 12 -%}
                                      </span>
                                      <span class="pl-15 heading-color">{{- link.title -}}</span>
                                    </div>
                                  </back-button>
                                  <div class="{{ section_width }}">
                                    {%- case lastBlock.type -%}
                                      {%- when 'menu_promotion' -%}
                                        {% assign banner_count = 0 %}
                                        {% for i in (1..2) %}
                                          {%- capture promotion_image -%}promotion_image_{{i}}{%- endcapture -%}
                                          {%- if lastBlock.settings[promotion_image] != blank -%}
                                            {% assign banner_count = banner_count | plus: 1 %}
                                          {%- endif -%}
                                        {% endfor %}
                                        <megamenu-inner
                                          class="d-flex flex-cols flex-mega-banner{% if lastBlock.settings.promotion_position == "left" %} row-reverse{% endif %}"
                                          style="--col-mega-width: {{ lastBlock.settings.promotion_image_width }}%"
                                        >
                                          {% if link.links.size > 0 %}
                                            <div class="flex-1 menu-list mt-1025-0-impo" style="--row-gap: 40px">
                                              {%- render 'horizontal-menu' | link: link | type: "mega" | column: lastBlock.settings.promotion_menu_column | is_mega: true -%}
                                            </div>
                                          {% endif %}
                                          {%- if lastBlock.settings.promotion_image_1 != blank
                                            or lastBlock.settings.promotion_image_2 != blank
                                          -%}
                                            <div class="custom-menu-column submenu-banner mt-30 mt-1025-0">
                                              <div
                                                class="mb-1 banner grid-cols gap-small tablet-2 row-as-column-gap"
                                                style="--col-number: {{ banner_count }}"
                                              >
                                                {%- if lastBlock.settings.promotion_image_1 != blank -%}
                                                  {%- assign banner_alt = lastBlock.settings.promotion_image_1.alt
                                                    | default: link.title
                                                    | escape
                                                  -%}
                                                  <a
                                                    {% if lastBlock.settings.promotion_link_1 == blank %}
                                                      role="link" aria-disabled="true"
                                                    {% else %}
                                                      aria-label="{{ link.title }}"
                                                      href="{{ lastBlock.settings.promotion_link_1 }}"
                                                    {% endif %}
                                                    class="p-0 header-banner rounded-corner-item"
                                                  >
                                                    {{
                                                      lastBlock.settings.promotion_image_1
                                                      | image_url: width: 600
                                                      | image_tag:
                                                        class: 'menu-banner-img rounded-corner-item',
                                                        widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                                        width: lastBlock.settings.promotion_image_1.width,
                                                        height: lastBlock.settings.promotion_image_1.height,
                                                        loading: 'lazy',
                                                        sizes: '100vw',
                                                        alt: banner_alt
                                                    }}
                                                  </a>
                                                {%- endif -%}
                                                {%- if lastBlock.settings.promotion_image_2 != blank -%}
                                                  {%- assign banner_alt = lastBlock.settings.promotion_image_2.alt
                                                    | default: link.title
                                                    | escape
                                                  -%}
                                                  <a
                                                    {% if lastBlock.settings.promotion_link_2 == blank %}
                                                      role="link" aria-disabled="true"
                                                    {% else %}
                                                      aria-label="{{ link.title }}"
                                                      href="{{ lastBlock.settings.promotion_link_2 }}"
                                                    {% endif %}
                                                    class="p-0 header-banner rounded-corner-item"
                                                  >
                                                    {{
                                                      lastBlock.settings.promotion_image_2
                                                      | image_url: width: 600
                                                      | image_tag:
                                                        class: 'menu-banner-img rounded-corner-item',
                                                        widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                                        width: lastBlock.settings.promotion_image_2.width,
                                                        height: lastBlock.settings.promotion_image_2.height,
                                                        loading: 'lazy',
                                                        sizes: '100vw',
                                                        alt: banner_alt
                                                    }}
                                                  </a>
                                                {%- endif -%}
                                              </div>
                                            </div>
                                          {%- endif -%}
                                        </megamenu-inner>
                                      {%- when 'menu_collection' -%}
                                        {% assign collection_count = 0 %}
                                        {% for i in (1..6) %}
                                          {%- capture collection -%}collection_{{i}}{%- endcapture -%}
                                          {%- if lastBlock.settings[collection] != blank -%}
                                            {% assign collection_count = collection_count | plus: 1 %}
                                          {%- endif -%}
                                        {% endfor %}
                                        <megamenu-inner
                                          class="d-flex flex-cols flex-mega-banner{% if collection_count > 4 and lastBlock.settings.collection_width > 70 %} flex-wrap{% endif %}{% if lastBlock.settings.collection_width > 85 or collection_count > 4 %} flex-wrap{% endif %}"
                                          style="{% if collection_count > 4 %} --col-mega-width: 100% {% else %}--col-mega-width: {{ lastBlock.settings.collection_width }}% {% endif %}"
                                        >
                                          {% if link.links.size > 0 %}
                                            <div
                                              class="menu-list flex-1{% if collection_count > 4 and lastBlock.settings.collection_width > 70 %} w-full{% endif %}{% if lastBlock.settings.collection_width > 85 or collection_count > 4 %} w-full{% endif %} mt-1025-0-impo"
                                              style="--row-gap: 40px"
                                            >
                                              {%- render 'horizontal-menu' | link: link | type: "mega" | column: lastBlock.settings.collection_menu_column | is_mega: true -%}
                                            </div>
                                          {% endif %}
                                          {%- if collection_count > 0 -%}
                                            <div class="custom-menu-column mt-30 mt-1025-0">
                                              <div
                                                class="row-as-column-gap grid-cols gap-small tablet-3-5 mb-1-5"
                                                style="--col-number: {{ collection_count }}"
                                              >
                                                {%- for i in (1..6) -%}
                                                  {%- capture collection -%}collection_{{i}}{%- endcapture -%}
                                                  {%- liquid
                                                    assign collection_value = lastBlock.settings[collection]
                                                  -%}
                                                  {%- if collection_value != blank -%}
                                                    {%- liquid
                                                      assign p_url = collection_value.url
                                                      assign p_img = collection_value.featured_image
                                                      assign product_ratio = '3/4'
                                                      if settings.product_image_ratio == 'adapt_to_image'
                                                        assign product_ratio = p_img.aspect_ratio
                                                      elsif settings.product_image_ratio == 'square'
                                                        assign product_ratio = '1/1'
                                                      endif
                                                    -%}
                                                    {%- capture style_ratio -%}
                                                    --aspect-ratio: 
                                                    {%- if p_img != blank -%} 
                                                      {%- if collection_value.image != blank -%}
                                                        {{ p_img.aspect_ratio }}
                                                      {%- else -%}
                                                        {{ product_ratio }}
                                                      {%- endif -%}
                                                    {%- else -%}
                                                      1/1
                                                    {% endif %}
                                                  {%- endcapture -%}
                                                    {%- if p_url != blank -%}
                                                      <div class="relative collection-items menu-collection rounded-corner-item">
                                                        {%- assign banner_alt = p_img.alt
                                                          | default: collection_value.title
                                                          | escape
                                                        -%}
                                                        <a
                                                          role="link"
                                                          aria-label="{{ collection_value.title }}"
                                                          href="{{ p_url }}"
                                                          class="banner__media collection-media p-0 hover-effect{% if settings.enable_zoom_effect %} zoom-in {% else %} fade-in {% endif %}"
                                                          style="{{ style_ratio | strip | strip_newlines }}"
                                                        >
                                                          {%- if p_img != blank -%}
                                                            {{
                                                              p_img
                                                              | image_url: width: 600
                                                              | image_tag:
                                                                class: 'menu-collection-img',
                                                                widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                                                width: p_img.width,
                                                                height: p_img.height,
                                                                loading: 'lazy',
                                                                sizes: '100vw',
                                                                alt: banner_alt
                                                            }}
                                                          {%- else -%}
                                                            {% render 'placeholder-render' | type: "collection" %}
                                                          {%- endif -%}
                                                          <div class="absolute inset-0 text-left collection-information mt-15 d-flex flex-column color-light px-15 pb-15 pb-sm-30 px-sm-30 pointer-none justify-content-end">
                                                            <span class="relative m-0 remove-underline h5 z-2">
                                                              {{- collection_value.title -}}
                                                            </span>
                                                          </div>
                                                        </a>
                                                      </div>
                                                    {%- endif -%}
                                                  {%- endif -%}
                                                {%- endfor -%}
                                              </div>
                                            </div>
                                          {%- endif -%}
                                        </megamenu-inner>
                                      {%- else -%}
                                        {% assign product_count = 0 %}
                                        {% for i in (1..3) %}
                                          {%- capture product -%}product_{{ i }}{%- endcapture -%}
                                          {%- if lastBlock.settings[product] != blank -%}
                                            {% assign product_count = product_count | plus: 1 %}
                                          {%- endif -%}
                                        {% endfor %}
                                        <megamenu-inner
                                          class="d-flex flex-cols flex-mega-banner"
                                          style="--col-mega-width: {{ lastBlock.settings.product_width }}%"
                                        >
                                          {% if link.links.size > 0 %}
                                            <div class="flex-1 menu-list mt-1025-0-impo" style="--row-gap: 40px">
                                              {%- render 'horizontal-menu' | link: link | type: "mega" | column: lastBlock.settings.product_menu_column | is_mega: true -%}
                                            </div>
                                          {% endif %}
                                          {%- if product_count > 0 -%}
                                            <div class="custom-menu-column mt-30 mt-1025-0 ">
                                              <div
                                                class="grid-cols gap-medium tablet-3-5 mb-1-5"
                                                style="--col-number: {{ product_count }}"
                                              >
                                                {%- for i in (1..3) -%}
                                                  {%- capture product -%}product_{{ i }}{%- endcapture -%}
                                                  {%- liquid
                                                    assign product_value = lastBlock.settings[product]
                                                  -%}
                                                  {%- if product_value != blank -%}
                                                    {% render 'card-product',
                                                      card_product: product_value,
                                                      disable_quickview: true
                                                    %}
                                                  {%- endif -%}
                                                {%- endfor -%}
                                              </div>
                                            </div>
                                          {%- endif -%}
                                        </megamenu-inner>
                                    {%- endcase -%}
                                  </div>
                                </div>
                              {%- endif -%}
                            </details>
                          </noscript>
                          <header-menu class="no-js-hidden">
                            <details id="Details-HeaderMenu-{{ forloop.index }}">
                              <summary
                                class="no-js-hidden menu__item-link first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative header-color first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                                aria-label="{{ link.title }}"
                              >
                                <span class="link_text">{{ link.title }}</span>
                                {%- if link.links.size > 0 or has_content -%}
                                  <span class="absolute inset-0 right-0 w-full! open-children-toggle touch-target currentColor-svg">
                                    {% render 'icon-arrow-down', custom_width: 12 %}
                                  </span>
                                {%- endif -%}
                              </summary>
                              {%- if link.links.size > 0 or has_content -%}
                                <div class="inset-x-0 sub-menu absolute-1025 bg-default">
                                  <back-button
                                    tabindex="0"
                                    class="relative mb-20 d-block d-1025-none fs-small mt-15 hidden-no-js"
                                  >
                                    <div class="pt-5 pb-3 accent-color-second-bg rounded-corner-item px-15 d-inline-flex align-center heading-color pointer">
                                      <span class="absolute inset-y-0 icon-back left-10 d-inline-flex align-center lh-1 currentColor-svg">
                                        {%- render 'icon-prev', custom_width: 12 -%}
                                      </span>
                                      <span class="pl-15 heading-color">{{- link.title -}}</span>
                                    </div>
                                  </back-button>
                                  <div class="{{ section_width }}">
                                    {%- case lastBlock.type -%}
                                      {%- when 'menu_promotion' -%}
                                        {% assign banner_count = 0 %}
                                        {% for i in (1..2) %}
                                          {%- capture promotion_image -%}promotion_image_{{i}}{%- endcapture -%}
                                          {%- if lastBlock.settings[promotion_image] != blank -%}
                                            {% assign banner_count = banner_count | plus: 1 %}
                                          {%- endif -%}
                                        {% endfor %}
                                        <megamenu-inner
                                          class="d-flex flex-cols flex-mega-banner{% if lastBlock.settings.promotion_position == "left" %} row-reverse{% endif %}"
                                          style="--col-mega-width: {{ lastBlock.settings.promotion_image_width }}%"
                                        >
                                          {% if link.links.size > 0 %}
                                            <div class="flex-1 menu-list mt-1025-0-impo" style="--row-gap: 40px">
                                              {%- render 'horizontal-menu' | link: link | type: "mega" | column: lastBlock.settings.promotion_menu_column | is_mega: true -%}
                                            </div>
                                          {% endif %}
                                          {%- if lastBlock.settings.promotion_image_1 != blank
                                            or lastBlock.settings.promotion_image_2 != blank
                                          -%}
                                            <div class="custom-menu-column submenu-banner mt-30 mt-1025-0">
                                              <div
                                                class="mb-1 banner grid-cols gap-small tablet-2 row-as-column-gap"
                                                style="--col-number: {{ banner_count }}"
                                              >
                                                {%- if lastBlock.settings.promotion_image_1 != blank -%}
                                                  {%- assign banner_alt = lastBlock.settings.promotion_image_1.alt
                                                    | default: link.title
                                                    | escape
                                                  -%}
                                                  <a
                                                    {% if lastBlock.settings.promotion_link_1 == blank %}
                                                      role="link" aria-disabled="true"
                                                    {% else %}
                                                      aria-label="{{ link.title }}"
                                                      href="{{ lastBlock.settings.promotion_link_1 }}"
                                                    {% endif %}
                                                    class="p-0 header-banner rounded-corner-item"
                                                  >
                                                    {{
                                                      lastBlock.settings.promotion_image_1
                                                      | image_url: width: 600
                                                      | image_tag:
                                                        class: 'menu-banner-img rounded-corner-item',
                                                        widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                                        width: lastBlock.settings.promotion_image_1.width,
                                                        height: lastBlock.settings.promotion_image_1.height,
                                                        loading: 'lazy',
                                                        sizes: '100vw',
                                                        alt: banner_alt
                                                    }}
                                                  </a>
                                                {%- endif -%}
                                                {%- if lastBlock.settings.promotion_image_2 != blank -%}
                                                  {%- assign banner_alt = lastBlock.settings.promotion_image_2.alt
                                                    | default: link.title
                                                    | escape
                                                  -%}
                                                  <a
                                                    {% if lastBlock.settings.promotion_link_2 == blank %}
                                                      role="link" aria-disabled="true"
                                                    {% else %}
                                                      aria-label="{{ link.title }}"
                                                      href="{{ lastBlock.settings.promotion_link_2 }}"
                                                    {% endif %}
                                                    class="p-0 header-banner rounded-corner-item"
                                                  >
                                                    {{
                                                      lastBlock.settings.promotion_image_2
                                                      | image_url: width: 600
                                                      | image_tag:
                                                        class: 'menu-banner-img rounded-corner-item',
                                                        widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                                        width: lastBlock.settings.promotion_image_2.width,
                                                        height: lastBlock.settings.promotion_image_2.height,
                                                        loading: 'lazy',
                                                        sizes: '100vw',
                                                        alt: banner_alt
                                                    }}
                                                  </a>
                                                {%- endif -%}
                                              </div>
                                            </div>
                                          {%- endif -%}
                                        </megamenu-inner>
                                      {%- when 'menu_collection' -%}
                                        {% assign collection_count = 0 %}
                                        {% for i in (1..6) %}
                                          {%- capture collection -%}collection_{{i}}{%- endcapture -%}
                                          {%- if lastBlock.settings[collection] != blank -%}
                                            {% assign collection_count = collection_count | plus: 1 %}
                                          {%- endif -%}
                                        {% endfor %}
                                        <megamenu-inner
                                          class="d-flex flex-cols flex-mega-banner{% if collection_count > 4 and lastBlock.settings.collection_width > 70 %} flex-wrap{% endif %}{% if lastBlock.settings.collection_width > 85 or collection_count > 4 %} flex-wrap{% endif %}"
                                          style="{% if collection_count > 4 %} --col-mega-width: 100% {% else %}--col-mega-width: {{ lastBlock.settings.collection_width }}% {% endif %}"
                                        >
                                          {% if link.links.size > 0 %}
                                            <div
                                              class="menu-list flex-1{% if collection_count > 4 and lastBlock.settings.collection_width > 70 %} w-full{% endif %}{% if lastBlock.settings.collection_width > 85 or collection_count > 4 %} w-full{% endif %} mt-1025-0-impo"
                                              style="--row-gap: 40px"
                                            >
                                              {%- render 'horizontal-menu' | link: link | type: "mega" | column: lastBlock.settings.collection_menu_column | is_mega: true -%}
                                            </div>
                                          {% endif %}
                                          {%- if collection_count > 0 -%}
                                            <div class="custom-menu-column mt-30 mt-1025-0">
                                              <div
                                                class="row-as-column-gap grid-cols gap-small tablet-3-5 mb-1-5"
                                                style="--col-number: {{ collection_count }}"
                                              >
                                                {%- for i in (1..6) -%}
                                                  {%- capture collection -%}collection_{{i}}{%- endcapture -%}
                                                  {%- liquid
                                                    assign collection_value = lastBlock.settings[collection]
                                                  -%}
                                                  {%- if collection_value != blank -%}
                                                    {%- liquid
                                                      assign p_url = collection_value.url
                                                      assign p_img = collection_value.featured_image
                                                      assign product_ratio = '3/4'
                                                      if settings.product_image_ratio == 'adapt_to_image'
                                                        assign product_ratio = p_img.aspect_ratio
                                                      elsif settings.product_image_ratio == 'square'
                                                        assign product_ratio = '1/1'
                                                      endif
                                                    -%}
                                                    {%- capture style_ratio -%}
                                                    --aspect-ratio: 
                                                    {%- if p_img != blank -%} 
                                                      {%- if collection_value.image != blank -%}
                                                        {{ p_img.aspect_ratio }}
                                                      {%- else -%}
                                                        {{ product_ratio }}
                                                      {%- endif -%}
                                                    {%- else -%}
                                                      1/1
                                                    {% endif %}
                                                  {%- endcapture -%}
                                                    {%- if p_url != blank -%}
                                                      <div class="relative collection-items menu-collection rounded-corner-item">
                                                        {%- assign banner_alt = p_img.alt
                                                          | default: collection_value.title
                                                          | escape
                                                        -%}
                                                        <a
                                                          role="link"
                                                          aria-label="{{ collection_value.title }}"
                                                          href="{{ p_url }}"
                                                          class="banner__media collection-media p-0 hover-effect{% if settings.enable_zoom_effect %} zoom-in {% else %} fade-in {% endif %}"
                                                          style="{{ style_ratio | strip | strip_newlines }}"
                                                        >
                                                          {%- if p_img != blank -%}
                                                            {{
                                                              p_img
                                                              | image_url: width: 600
                                                              | image_tag:
                                                                class: 'menu-collection-img',
                                                                widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                                                width: p_img.width,
                                                                height: p_img.height,
                                                                loading: 'lazy',
                                                                sizes: '100vw',
                                                                alt: banner_alt
                                                            }}
                                                          {%- else -%}
                                                            {% render 'placeholder-render' | type: "collection" %}
                                                          {%- endif -%}
                                                          <div class="absolute inset-0 text-left collection-information mt-15 d-flex flex-column color-light px-15 pb-15 pb-sm-30 px-sm-30 pointer-none justify-content-end">
                                                            <span class="relative m-0 remove-underline h5 z-2">
                                                              {{- collection_value.title -}}
                                                            </span>
                                                          </div>
                                                        </a>
                                                      </div>
                                                    {%- endif -%}
                                                  {%- endif -%}
                                                {%- endfor -%}
                                              </div>
                                            </div>
                                          {%- endif -%}
                                        </megamenu-inner>
                                      {%- else -%}
                                        {% assign product_count = 0 %}
                                        {% for i in (1..3) %}
                                          {%- capture product -%}product_{{ i }}{%- endcapture -%}
                                          {%- if lastBlock.settings[product] != blank -%}
                                            {% assign product_count = product_count | plus: 1 %}
                                          {%- endif -%}
                                        {% endfor %}
                                        <megamenu-inner
                                          class="d-flex flex-cols flex-mega-banner"
                                          style="--col-mega-width: {{ lastBlock.settings.product_width }}%"
                                        >
                                          {% if link.links.size > 0 %}
                                            <div class="flex-1 menu-list mt-1025-0-impo" style="--row-gap: 40px">
                                              {%- render 'horizontal-menu' | link: link | type: "mega" | column: lastBlock.settings.product_menu_column | is_mega: true -%}
                                            </div>
                                          {% endif %}
                                          {%- if product_count > 0 -%}
                                            <div class="custom-menu-column mt-30 mt-1025-0 ">
                                              <div
                                                class="grid-cols gap-medium tablet-3-5 mb-1-5"
                                                style="--col-number: {{ product_count }}"
                                              >
                                                {%- for i in (1..3) -%}
                                                  {%- capture product -%}product_{{ i }}{%- endcapture -%}
                                                  {%- liquid
                                                    assign product_value = lastBlock.settings[product]
                                                  -%}
                                                  {%- if product_value != blank -%}
                                                    {% render 'card-product',
                                                      card_product: product_value,
                                                      disable_quickview: true
                                                    %}
                                                  {%- endif -%}
                                                {%- endfor -%}
                                              </div>
                                            </div>
                                          {%- endif -%}
                                        </megamenu-inner>
                                    {%- endcase -%}
                                  </div>
                                </div>
                              {%- endif -%}
                            </details>
                          </header-menu>
                        </li>
                        {%- assign is_mega_menu = true -%}
                      {% endif %}
                      {%- if is_mega_menu == false -%}
                        <li class="menu-link{% if link.links.size > 0 %} have-child classic{% endif %}" role="listitem">
                          <noscript>
                            <details>
                              <summary
                                class="menu__item-link header-color-lg first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                              >
                                <span class="link_text">{{ link.title }}</span>
                                {% if link.links.size > 0 %}
                                  <span class="absolute inset-0 right-0 w-full! open-children-toggle touch-target currentColor-svg">
                                    {% render 'icon-arrow-down', custom_width: 12 %}
                                  </span>
                                {% endif %}
                              </summary>
                              {%- render 'horizontal-menu' | link: link | sub: "sub-menu" | type: "classic" -%}
                            </details>
                          </noscript>
                          {%- if link.links.size > 0 -%}
                            <header-menu>
                              <details id="Details-HeaderMenu-{{ forloop.index }}">
                                <summary
                                  id="HeaderMenu-{{ link.handle }}"
                                  class="no-js-hidden menu__item-link first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative header-color first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                                  aria-label="{{ link.title }}"
                                >
                          {%- else -%}
                            <a
                              class="no-js-hidden menu__item-link first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative header-color first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                              href="{{ link.url }}"
                              aria-label="{{ link.title }}"
                            >
                          {%- endif -%}
                          <span class="link_text">{{ link.title }}</span>
                          {% if link.links.size > 0 %}
                            <span class="absolute inset-0 right-0 w-full! open-children-toggle touch-target currentColor-svg">
                              {% render 'icon-arrow-down', custom_width: 12 %}
                            </span>
                          {% endif %}
                          {%- if link.links.size > 0 -%}
                            </summary>
                          {%- else -%}
                            </a>
                          {%- endif -%}
                          {%- render 'horizontal-menu' | link: link | sub: "sub-menu" | type: "classic" | level1_index: forloop.index -%}
                          {%- if link.links.size > 0 -%}
                            </details>
                            </header-menu>
                          {%- endif -%}
                        </li>
                      {%- endif -%}
                    {%- else -%}
                      <li class="menu-link{% if link.links.size > 0 %} have-child classic{% endif %}" role="listitem">
                        <noscript>
                          <details>
                            <summary
                              class="menu__item-link header-color-lg first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                            >
                              <span class="link_text">{{ link.title }}</span>
                              {% if link.links.size > 0 %}
                                <span class="absolute inset-0 right-0 w-full! open-children-toggle touch-target currentColor-svg">
                                  {% render 'icon-arrow-down', custom_width: 12 %}
                                </span>
                              {% endif %}
                            </summary>
                            {%- render 'horizontal-menu' | link: link | sub: "sub-menu" | type: "classic" -%}
                          </details>
                        </noscript>
                        {%- if link.links.size > 0 -%}
                          <header-menu>
                            <details id="Details-HeaderMenu-{{ forloop.index }}">
                              <summary
                                id="HeaderMenu-{{ link.handle }}"
                                class="no-js-hidden menu__item-link first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative header-color first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                                aria-label="{{ link.title }}"
                              >
                        {%- else -%}
                          <a
                            class="no-js-hidden menu__item-link first-level{% if settings.menu_font == 'heading_font' %} heading-font font-smoothing-initial{% endif %} py-15 py-1025-2 relative header-color first-child-js remove-underline{% if link.links.size > 0 %} pr-1024-44{% endif %}{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                            href="{{ link.url }}"
                            aria-label="{{ link.title }}"
                          >
                        {%- endif -%}

                        <span class="link_text">{{ link.title }}</span>
                        {%- if link.links.size > 0 -%}
                          <span class="absolute inset-0 right-0 w-full! open-children-toggle touch-target currentColor-svg">
                            {% render 'icon-arrow-down', custom_width: 12 %}
                          </span>
                          </summary>
                        {%- else -%}
                          </a>
                        {%- endif -%}
                        {%- render 'horizontal-menu' | link: link | sub: "sub-menu" | type: "classic" | level1_index: forloop.index -%}
                        {%- if link.links.size > 0 -%}
                          </details>
                          </header-menu>
                        {%- endif -%}
                      </li>
                    {%- endif -%}
                  {%- endfor -%}
                </ul>
                {%- if section_st.banner_type != 'none' -%}
                  {% if section_st.banner_collection != blank or section_st.banner_image != blank %}
                    <div class="menu-mobile-block d-block d-1025-none pb-35">
                      {%- if section_st.banner_title != blank -%}
                        <p class="heading-color full-width-gap{% if section_st.uppercase_first_level %} uppercase{% endif %}">
                          {{ section_st.banner_title }}
                        </p>
                      {%- endif -%}
                      {% if section_st.banner_type != 'collection' %}
                        <div class="mb-1 banner gap-small tablet-1 row-as-column-gap full-width-gap">
                          {%- assign banner_default = 'sections.header.mobile_banner' | t -%}
                          {%- assign banner_alt = section_st.banner_image.alt | default: banner_default | escape -%}
                          <a
                            {% if section_st.banner_link == blank %}
                              role="link" aria-disabled="true"
                            {% else %}
                              aria-label="{{ banner_default }}" href="{{ section_st.banner_link }}"
                            {% endif %}
                            class="p-0 header-banner rounded-corner-item"
                          >
                            {{
                              section_st.banner_image
                              | image_url: width: 600
                              | image_tag:
                                class: 'menu-banner-img rounded-corner-item',
                                widths: '50, 100, 150, 200, 250, 300, 400, 500, 600, 700',
                                width: section_st.banner_image.width,
                                height: section_st.banner_image.height,
                                loading: 'lazy',
                                sizes: '100vw',
                                alt: banner_alt
                            }}
                          </a>
                        </div>
                      {%- else -%}
                        <div class="grid-cols row-as-column-gap gap-small tablet-3-5 mb-1-5 full-width-gap">
                          {%- for collection in section_st.banner_collection -%}
                            <div class="relative collection-items align-self-start">
                              <div class="relative collection__inner">
                                <div
                                  class="banner__media collection-media"
                                  style=" --aspect-ratio: 3/4; "
                                >
                                  {%- liquid
                                    assign p_url = collection.url
                                    assign p_img = collection.featured_image
                                  -%}
                                  {%- assign banner_alt = collection.title | escape -%}
                                  <a
                                    class="relative m-0 remove-underline h5 z-2 fade-in"
                                    {% if p_url == blank %}
                                      role="link" aria-disabled="true"
                                    {% else %}
                                      href="{{ p_url }}"
                                    {% endif %}
                                    aria-label="{{ banner_alt }}"
                                    style="--aspect-ratio: 3/4"
                                  >
                                    {%- if p_img -%}
                                      {{
                                        p_img
                                        | image_url: width: 750
                                        | image_tag:
                                          loading: 'lazy',
                                          sizes: '100vw',
                                          widths: '165, 360, 535, 750, 1070, 1500',
                                          alt: banner_alt
                                      }}
                                    {%- else -%}
                                      {% render 'placeholder-render' | type: "collection" %}
                                    {%- endif -%}
                                  </a>
                                </div>
                                <div class="absolute inset-0 text-left collection-information mt-15 d-flex flex-column color-light px-15 pb-15 pb-sm-30 px-sm-30 pointer-none justify-content-end">
                                  {%- liquid
                                    assign p_url = ''
                                    assign p_url = collection.url
                                  -%}
                                  <a
                                    class="relative m-0 remove-underline h5 z-2"
                                    {% if p_url == blank %}
                                      role="link" aria-disabled="true"
                                    {% else %}
                                      href="{{ p_url }}"
                                    {% endif %}
                                    aria-label="{{ collection.title }}"
                                  >
                                    {{ collection.title }}
                                  </a>
                                  <div class="relative mt-2 product-count z-2 lg:text-base text-sm">
                                    {%- assign pc = collection.products_count | default: 0 -%}
                                    {{ pc }}
                                    {%- if pc < 2 -%}
                                      {{ 'sections.collection_list.item' | t }}
                                    {%- else -%}
                                      {{ 'sections.collection_list.items' | t }}
                                    {%- endif -%}
                                  </div>
                                </div>
                              </div>
                            </div>
                          {%- endfor -%}
                        </div>
                      {% endif %}
                    </div>
                  {% endif %}
                {%- endif -%}
                {%- if section_st.show_language or section_st.show_currency or section_st.show_account -%}
                  <div class="sticky inset-x-0 bottom-0 header__action-mobile d-1025-none bg-default z-2 pt-0-impo">
                    <div class="gap-10 pt-20 border-top align-center justify-content-end d-flex space-between">
                      {% if section_st.show_language or section_st.show_currency %}
                        <div class="desktop-localization-wrapper last-r-0 pr-30 d-flex gap-15 align-center">
                          {% if section_st.show_currency %}
                            {%- render 'country-localization', localPosition: 'HeaderCountryMobile' -%}
                          {% endif %}
                          {% if section_st.show_language %}
                            {%- render 'language-localization', localPosition: 'HeaderLanguageMobile' -%}
                          {% endif %}
                        </div>
                      {% endif %}
                      {%- if section_st.show_account -%}
                        <a
                          href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
                          class="header-account link tooltip mx-3 last-r-0 remove-underline{% if section_st.uppercase_first_level %} uppercase{% endif %}"
                          aria-label="{{ 'customer.account_fallback' | t }}"
                        >
                          <span class="">
                            {%- liquid
                              if customer
                                echo 'customer.account_fallback' | t
                              else
                                echo 'customer.log_in' | t
                              endif
                            -%}
                          </span>
                        </a>
                      {%- endif -%}
                    </div>
                  </div>
                {%- endif -%}
              </div>
            </div>
          </main-menu>
        </nav>
      {%- endif -%}
      {%- if section_st.show_account
        or section_st.show_search
        or section_st.show_cart
        or section_st.show_language
        or section_st.show_currency
      -%}
        <div class="header__action d-flex align-center justify-content-end">
          {% if section_st.show_language or section_st.show_currency %}
            <div class="desktop-localization-wrapper last-r-0 pl-10 d-none d-1025-flex align-center gap-15 pr-30 header-color{% if settings.menu_font == 'heading_font' %} use-heading-font{% endif %}">
              {% if section_st.show_currency %}
                {%- render 'country-localization', localPosition: 'HeaderCountry' -%}
              {% endif %}
              {% if section_st.show_language %}
                {%- render 'language-localization', localPosition: 'HeaderLanguage' -%}
              {% endif %}
            </div>
          {% endif %}
          {%- if section_st.show_search -%}
            <a
              href="{{ routes.search_url }}"
              class="mx-3 header__icon header__icon--search link focus-inset tooltip last-r-0 element-focus"
              id="search-icon-bubble"
              data-custom-class="{{ section_width }}"
            >
              <span class="tooltip-content fs-small tooltip-bottom">
                {{ 'general.search.search' | t }}
              </span>
              {% render 'icon-search', custom_width: 22 %}

              <span class="visually-hidden">{{ 'general.search.search' | t }}</span>
            </a>
          {%- endif -%}
          {%- if section_st.show_account -%}
            <a
              href="{%- if customer -%}{{ routes.account_url }}{%- else -%}{{ routes.account_login_url }}{%- endif -%}"
              class="mx-3 header__icon header-account link tooltip last-r-0 d-none d-1025-inline-flex"
              aria-label="{{ 'customer.account_fallback' | t }}"
            >
              {% render 'icon-account', custom_width: 25 %}
              <span class="tooltip-content fs-small tooltip-bottom">
                {%- liquid
                  if customer
                    echo 'customer.account_fallback' | t
                  else
                    echo 'customer.log_in' | t
                  endif
                -%}
              </span>
            </a>
          {%- endif -%}
          {%- if section_st.show_cart -%}
            <a
              href="{{ routes.cart_url }}"
              class="header__icon--cart d-inline-flex {% if template != 'cart' %}icon--cart-action{% endif %} link focus-inset tooltip last-r-0 remove-underline"
              id="cart-icon-bubble"
            >
              <span class="header__icon">
                {%- if settings.cart_icon == 'shopping_bag' -%}
                  {% render 'icon-bag', custom_width: 20 %}
                {%- else -%}
                  {% render 'icon-cart', custom_width: 25 %}
                {%- endif -%}
              </span>
              <span class="tooltip-content fs-small tooltip-bottom">
                {{ 'templates.cart.minicart' | t }}
              </span>
              <span class="visually-hidden">{{ 'templates.cart.cart' | t }}</span>
              {%- if cart != empty -%}
                <div class="cart-count round-50 bg-dark color-light fs-small d-inline-flex align-self-center">
                  {%- if cart.item_count < 100 -%}
                    <span>{{ cart.item_count }}</span>
                  {%- else -%}
                    <span aria-hidden="true">~</span>
                  {%- endif -%}
                  <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
                </div>
              {%- else -%}
                <div class="cart-count round-50 bg-dark color-light fs-small d-inline-flex">
                  <span aria-hidden="true">0</span>
                </div>
              {%- endif -%}
            </a>
          {%- endif -%}
        </div>
      {%- endif -%}
      {%- if settings.cart_type == 'added_message' -%}
        {%- render 'cart-notification' -%}
      {%- endif -%}
    </header-inner>
  </div>
</header>
{% if section_st.sticky_header_type != 'none' %}</sticky-header>{% else %}</div>{% endif %}
{% javascript %}
  class StickyHeader extends HTMLElement {
    constructor() {
      super();
    }

    connectedCallback() {
      this.header = document.querySelector('.section-header');
      this.headerIsAlwaysSticky = this.getAttribute('data-sticky-type') === 'always';
      this.headerBounds = {};

      this.setHeaderHeight();

      window.matchMedia('(max-width: 990px)').addEventListener('change', this.setHeaderHeight.bind(this));

      if (this.headerIsAlwaysSticky) {
        this.header.classList.add('shopify-section-header-sticky');
      }

      this.currentScrollTop = 0;
      this.preventReveal = false;
      this.predictiveSearch = this.querySelector('predictive-search');

      this.onScrollHandler = this.onScroll.bind(this);
      this.hideHeaderOnScrollUp = () => (this.preventReveal = true);

      this.addEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.addEventListener('scroll', this.onScrollHandler, false);

      this.createObserver();
    }

    setHeaderHeight() {
      document.documentElement.style.setProperty('--header-height', `${this.header.offsetHeight}px`);
    }

    disconnectedCallback() {
      this.removeEventListener('preventHeaderReveal', this.hideHeaderOnScrollUp);
      window.removeEventListener('scroll', this.onScrollHandler);
    }

    createObserver() {
      let observer = new IntersectionObserver((entries, observer) => {
        this.headerBounds = entries[0].intersectionRect;
        observer.disconnect();
      });

      observer.observe(this.header);
    }

    onScroll() {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;

      if (this.predictiveSearch && this.predictiveSearch.isOpen) return;

      if (scrollTop > this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (this.preventHide) return;
        requestAnimationFrame(this.hide.bind(this));
      } else if (scrollTop < this.currentScrollTop && scrollTop > this.headerBounds.bottom) {
        this.header.classList.add('scrolled-past-header');
        if (!this.preventReveal) {
          requestAnimationFrame(this.reveal.bind(this));
        } else {
          window.clearTimeout(this.isScrolling);

          this.isScrolling = setTimeout(() => {
            this.preventReveal = false;
          }, 66);

          requestAnimationFrame(this.hide.bind(this));
        }
      } else if (scrollTop <= this.headerBounds.top) {
        this.header.classList.remove('scrolled-past-header');
        requestAnimationFrame(this.reset.bind(this));
      }

      this.currentScrollTop = scrollTop;
    }

    hide() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-hidden', 'shopify-section-header-sticky');
    }

    reveal() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.add('shopify-section-header-sticky', 'animate');
      this.header.classList.remove('shopify-section-header-hidden');
    }

    reset() {
      if (this.headerIsAlwaysSticky) return;
      this.header.classList.remove('shopify-section-header-hidden', 'shopify-section-header-sticky', 'animate');
    }
  }

  customElements.define('sticky-header', StickyHeader);
{% endjavascript %}
<script type="application/ld+json">
  {
    "@context": "http://schema.org",
    "@type": "Organization",
    "name": {{ shop.name | json }},
    {% if section_st.logo %}
      "logo": {{ section_st.logo | image_url: width: 500 | prepend: "https:" | json }},
    {% endif %}
    "sameAs": [
      {{ settings.social_media_twitter | json }},
      {{ settings.social_media_facebook | json }},
      {{ settings.social_media_instagram | json }},
      {{ settings.social_media_tiktok | json }},
      {{ settings.social_media_pinterest | json }},
      {{ settings.social_media_youtube | json }},
      {{ settings.social_media_vimeo | json }},
      {{ settings.social_media_linkedin | json }},
      {{ settings.social_media_whatsapp | json }},
      {{ settings.social_media_snapchat | json }}
    ],
    "url": {{ request.origin | append: page.url | json }}
  }
</script>
{%- if request.page_type == 'index' -%}
  {% assign potential_action_target = request.origin | append: routes.search_url | append: '?q={search_term_string}' %}
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "WebSite",
      "name": {{ shop.name | json }},
      "potentialAction": {
        "@type": "SearchAction",
        "target": {{ potential_action_target | json }},
        "query-input": "required name=search_term_string"
      },
      "url": {{ request.origin | append: page.url | json }}
    }
  </script>
{%- endif -%}
{% schema %}
{
  "name": "t:sections.header.name",
  "class": "section-header",
  "settings": [
    {
      "type": "select",
      "id": "section_width",
      "options": [
        {
          "value": "fix_width",
          "label": "t:sections.all.layout.fix_width"
        },
        {
          "value": "stretch_width",
          "label": "t:sections.all.layout.stretch_width"
        },
        {
          "value": "expand_full_width",
          "label": "t:sections.all.layout.expand_full_width"
        }
      ],
      "default": "stretch_width",
      "label": "t:sections.all.layout.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "default-color-scheme"
    },
    {
      "type": "select",
      "id": "logo_position",
      "options": [
        {
          "value": "left",
          "label": "t:sections.header.settings.logo_position.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.header.settings.logo_position.options__2.label"
        },
        {
          "value": "menu-left",
          "label": "t:sections.header.settings.logo_position.options__3.label"
        }
      ],
      "default": "left",
      "info": "t:sections.header.settings.logo_position.info",
      "label": "t:sections.header.settings.logo_position.label"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "t:sections.header.settings.logo.label"
    },
    {
      "type": "range",
      "id": "logo_width",
      "min": 50,
      "max": 250,
      "step": 2,
      "default": 178,
      "unit": "t:sections.header.settings.logo_width.unit",
      "label": "t:sections.header.settings.logo_width.label"
    },
    {
      "type": "range",
      "id": "logo_width_mobile",
      "min": 50,
      "max": 150,
      "step": 2,
      "default": 100,
      "unit": "t:sections.header.settings.logo_width_mobile.unit",
      "label": "t:sections.header.settings.logo_width_mobile.label"
    },
    {
      "type": "select",
      "id": "sticky_header_type",
      "options": [
        {
          "value": "none",
          "label": "t:sections.header.settings.sticky_header_type.options__1.label"
        },
        {
          "value": "on-scroll-up",
          "label": "t:sections.header.settings.sticky_header_type.options__2.label"
        },
        {
          "value": "always",
          "label": "t:sections.header.settings.sticky_header_type.options__3.label"
        }
      ],
      "default": "on-scroll-up",
      "label": "t:sections.header.settings.sticky_header_type.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.menu.header"
    },
    {
      "type": "link_list",
      "id": "menu",
      "default": "main-menu",
      "label": "t:sections.header.settings.menu.label"
    },
    {
      "type": "checkbox",
      "id": "uppercase_first_level",
      "label": "t:sections.header.settings.menu.uppercase",
      "default": true
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.icon_header.content"
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "default": true,
      "label": "t:sections.header.settings.show_search.label"
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "default": true,
      "label": "t:sections.header.settings.show_account.label"
    },
    {
      "type": "checkbox",
      "id": "show_cart",
      "default": true,
      "label": "t:sections.header.settings.show_cart.label"
    },
    {
      "type": "checkbox",
      "id": "show_currency",
      "default": true,
      "label": "t:sections.header.settings.show_currency.label"
    },
    {
      "type": "checkbox",
      "id": "show_language",
      "default": false,
      "label": "t:sections.header.settings.show_language.label"
    },
    {
      "type": "header",
      "content": "t:sections.header.settings.mobile_settings.header"
    },
    {
      "type": "select",
      "id": "banner_type",
      "options": [
        {
          "value": "collection",
          "label": "t:sections.header.settings.mobile_settings.media_type.option_1"
        },
        {
          "value": "banner",
          "label": "t:sections.header.settings.mobile_settings.media_type.option_2"
        },
        {
          "value": "none",
          "label": "t:sections.header.settings.mobile_settings.media_type.option_3"
        }
      ],
      "default": "collection",
      "label": "t:sections.header.settings.mobile_settings.media_type.label"
    },
    {
      "type": "text",
      "id": "banner_title",
      "label": "t:sections.header.settings.mobile_settings.banner_title",
      "default": "Popular categories"
    },
    {
      "type": "collection_list",
      "id": "banner_collection",
      "label": "t:sections.header.settings.mobile_settings.collection"
    },
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "t:sections.header.settings.mobile_settings.banner_img"
    },
    {
      "type": "url",
      "id": "banner_link",
      "label": "t:sections.header.settings.mobile_settings.link"
    },
    {
      "type": "checkbox",
      "id": "show_border_bottom",
      "default": true,
      "label": "t:sections.header.settings.show_border_bottom.label"
    },
    {
      "type": "header",
      "content": "t:settings_schema.color.settings.header.transparent"
    },
    {
      "type": "checkbox",
      "id": "transparent_on_home",
      "default": false,
      "label": "t:settings_schema.color.settings.header.transparent_on_home",
      "info": "t:settings_schema.color.settings.header.info"
    },
    {
      "type": "checkbox",
      "id": "transparent_on_mobile",
      "default": false,
      "label": "t:settings_schema.color.settings.header.enable_mobile"
    },
    {
      "type": "image_picker",
      "id": "logo_transparent",
      "label": "t:sections.header.settings.logo.label"
    },
    {
      "type": "color",
      "id": "header_text_icon_transparent",
      "label": "t:settings_schema.color.settings.header.text_icon_transparent",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "menu_promotion",
      "name": "t:sections.header.settings.blocks.promotion.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "t:sections.header.settings.blocks.menu_item.label",
          "info": "t:sections.header.settings.blocks.menu_item.info"
        },
        {
          "type": "select",
          "id": "promotion_position",
          "options": [
            {
              "value": "left",
              "label": "t:sections.header.settings.blocks.promotion.position.left"
            },
            {
              "value": "right",
              "label": "t:sections.header.settings.blocks.promotion.position.right"
            }
          ],
          "default": "right",
          "label": "t:sections.header.settings.blocks.promotion.position.label"
        },
        {
          "type": "range",
          "id": "promotion_menu_column",
          "min": 2,
          "max": 5,
          "step": 1,
          "label": "t:sections.header.settings.blocks.menu_column.label",
          "default": 4
        },
        {
          "type": "header",
          "content": "t:sections.header.settings.blocks.promotion.label"
        },
        {
          "type": "range",
          "id": "promotion_image_width",
          "min": 20,
          "max": 50,
          "unit": "%",
          "step": 1,
          "label": "t:sections.header.settings.blocks.promotion.upload_image_width",
          "default": 27
        },
        {
          "type": "header",
          "content": "t:sections.header.settings.blocks.promotion.promotion_1.label"
        },
        {
          "type": "image_picker",
          "id": "promotion_image_1",
          "label": "t:sections.header.settings.blocks.promotion.upload_image"
        },
        {
          "type": "url",
          "id": "promotion_link_1",
          "label": "t:sections.header.settings.blocks.promotion.promotion_1.link"
        },
        {
          "type": "header",
          "content": "t:sections.header.settings.blocks.promotion.promotion_2.label"
        },
        {
          "type": "image_picker",
          "id": "promotion_image_2",
          "label": "t:sections.header.settings.blocks.promotion.upload_image"
        },
        {
          "type": "url",
          "id": "promotion_link_2",
          "label": "t:sections.header.settings.blocks.promotion.promotion_2.link"
        }
      ]
    },
    {
      "type": "menu_product",
      "name": "t:sections.header.settings.blocks.product.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "t:sections.header.settings.blocks.menu_item.label",
          "info": "t:sections.header.settings.blocks.menu_item.info"
        },
        {
          "type": "range",
          "id": "product_menu_column",
          "min": 2,
          "max": 5,
          "step": 1,
          "label": "t:sections.header.settings.blocks.menu_column.label",
          "default": 4
        },
        {
          "type": "header",
          "content": "t:sections.header.settings.blocks.product.all.header"
        },
        {
          "type": "range",
          "id": "product_width",
          "min": 30,
          "max": 50,
          "unit": "%",
          "step": 1,
          "label": "t:sections.header.settings.blocks.product.width",
          "default": 50
        },
        {
          "type": "product",
          "id": "product_1",
          "label": "t:sections.header.settings.blocks.product.product_1"
        },
        {
          "type": "product",
          "id": "product_2",
          "label": "t:sections.header.settings.blocks.product.product_2"
        }
      ]
    },
    {
      "type": "menu_collection",
      "name": "t:sections.header.settings.blocks.collection.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "t:sections.header.settings.blocks.menu_item.label",
          "info": "t:sections.header.settings.blocks.menu_item.info"
        },
        {
          "type": "range",
          "id": "collection_menu_column",
          "min": 2,
          "max": 5,
          "step": 1,
          "label": "t:sections.header.settings.blocks.menu_column.label",
          "default": 4
        },
        {
          "type": "header",
          "content": "t:sections.header.settings.blocks.collection.all.header"
        },
        {
          "type": "range",
          "id": "collection_width",
          "min": 30,
          "max": 100,
          "unit": "%",
          "step": 1,
          "label": "t:sections.header.settings.blocks.collection.width",
          "default": 100,
          "info": "t:sections.header.settings.blocks.collection.info"
        },
        {
          "type": "collection",
          "id": "collection_1",
          "label": "t:sections.header.settings.blocks.collection.collection_1"
        },
        {
          "type": "collection",
          "id": "collection_2",
          "label": "t:sections.header.settings.blocks.collection.collection_2"
        },
        {
          "type": "collection",
          "id": "collection_3",
          "label": "t:sections.header.settings.blocks.collection.collection_3"
        },
        {
          "type": "collection",
          "id": "collection_4",
          "label": "t:sections.header.settings.blocks.collection.collection_4"
        },
        {
          "type": "collection",
          "id": "collection_5",
          "label": "t:sections.header.settings.blocks.collection.collection_5"
        },
        {
          "type": "collection",
          "id": "collection_6",
          "label": "t:sections.header.settings.blocks.collection.collection_6"
        }
      ]
    }
  ],
  "default": {
    "blocks": [
      {
        "type": "menu_promotion"
      }
    ]
  }
}
{% endschema %}